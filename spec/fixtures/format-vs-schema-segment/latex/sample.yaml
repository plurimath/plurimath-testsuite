schema: plurimath-corpus/asciimath/1
group: sample
description: "Healthy template with one planted defect: an input_format that contradicts the schema declaration."
input_format: latex
targets:
- asciimath
cases:
- id: number-integer
  input: "2"
  input_format: latex
  preprocessed: "2"
  expected:
    asciimath: "2"
  parse_tree:
    number: "2"
  model:
    class: Math::Formula
    fields:
      value:
      - class: Math::Number
        fields:
          value: "2"
