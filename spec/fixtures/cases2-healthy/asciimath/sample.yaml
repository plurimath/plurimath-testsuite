schema: plurimath-corpus/asciimath/2
group: sample
description: Healthy cases/2 payload, carrying no defect.
input_format: asciimath
targets:
- asciimath
- unicodemath
cases:
- id: sqrt-unclosed
  input: sqrt(
  input_format: asciimath
  preprocessed: sqrt(
  expected:
    asciimath:
      output: sqrt
    unicodemath:
      error:
        category: parse_error
  parse_tree:
    unary_class: sqrt
  model:
    class: Math::Formula
    fields:
      value:
      - class: Math::Function::Sqrt
        fields:
          parameter_one:
