schema: plurimath-corpus/asciimath/1
group: numbers
description: "Healthy template with one planted defect: a case id with an embedded newline."
input_format: asciimath
targets:
- asciimath
cases:
- id: "abc\nxyz"
  input: "2"
  input_format: asciimath
  preprocessed: "2"
  expected:
    asciimath: "2"
  parse_tree:
    number: "2"
  model:
    class: Math::Formula
    fields:
      value:
      - class: Math::Number
        fields:
          value: "2"
