schema: plurimath-corpus/docx/1
group: numbers
description: "Healthy template with one planted defect: a schema declaration no schema file claims."
input_format: asciimath
targets:
- asciimath
cases:
- id: number-integer
  input: "2"
  input_format: asciimath
  preprocessed: "2"
  expected:
    asciimath: "2"
  parse_tree:
    number: "2"
  model:
    class: Math::Formula
    fields:
      value:
      - class: Math::Number
        fields:
          value: "2"
