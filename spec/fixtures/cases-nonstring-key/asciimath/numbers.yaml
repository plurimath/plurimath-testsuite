schema: plurimath-corpus/asciimath/1
group: numbers
description: "Healthy template with one planted defect: a mapping key that is not a string."
input_format: asciimath
targets:
- asciimath
cases:
- id: number-integer
  input: "2"
  input_format: asciimath
  preprocessed: "2"
  expected:
    asciimath: "2"
  parse_tree:
    number: "2"
  model:
    class: Math::Formula
    fields:
      value:
      - class: Math::Number
        fields:
          value: "2"
1: true
